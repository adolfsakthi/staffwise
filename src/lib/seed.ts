import { Firestore } from 'firebase/firestore';

/**
 * The seed script is currently disabled.
 * It will be re-enabled and updated based on new instructions.
 */
export async function seedDatabase(db: Firestore, clientId: string, branchId: string) {
  console.log('Database seeding is currently disabled.');
  return Promise.resolve();
}
